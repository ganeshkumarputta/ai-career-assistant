import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aicareerassistant.app',
  appName: 'AI Career Assistant',
  webDir: 'build'
};

export default config;
